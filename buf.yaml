version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  ignore:
    # This compatibility shim intentionally keeps Buf's canonical package name.
    - proto/buf/validate
breaking:
  use:
    - FILE
